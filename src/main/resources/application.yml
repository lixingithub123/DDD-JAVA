server:
  # ??
  port: 8001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/automated_java_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=GMT%2b8
    username: root
    password: mysql_PjAFkh
  data:
    redis:
      database: 1
      host: 120.48.5.80
      port: 6379
      password: redis_b3YetY
      timeout: 10s
    lettuce:
      pool:
        # ????????
        max-active: 200
        # ???????????????????????
        max-wait: -1ms
        # ???????????
        max-idle: 10
        # ???????????
        min-idle: 0

############## Sa-Token ?? (??: https://sa-token.cc) ##############
sa-token:
  # token ??????? cookie ???
  token-name: satoken
  # token ????????? ??30??-1 ??????
  timeout: 2592000
  # token ??????????????? token ???????????????????-1 ??????????
  active-timeout: -1
  # ?????????????? ?? true ???????, ? false ??????????
  is-concurrent: true
  # ????????????????? token ?? true ????????? token, ? false ????????? token?
  is-share: true
  # token ?????????uuid?simple-uuid?random-32?random-64?random-128?tik?
  token-style: uuid
  # ????????
  is-log: true

# 开发环境 application-dev.yml
auto-table:
  # 开启自动建表
  enable: true
  record-sql:
    # 开启SQL记录
    enable: true
    # 当前版本（此处可保持与计划上线的版本号一致，方便管理SQL文件）
    version: 1.0.0
    # 记录到数据库（如果是多数据源，则会根据不同数据源，记录到不同的库）
    record-type: db


